{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Desktop",
            "type": "shell",
            "command": "msbuild /property:Configuration=Release MyGame.Desktop.csproj && mono bin/DesktopGL/'Any CPU'/Release/MyGame.Desktop.exe",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}